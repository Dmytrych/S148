'use client'

// TODO: Move all variables here
export const config = {
  serverBackendUrl: process.env.BACK_END_URL ?? "",
  backendUrl: process.env.NEXT_PUBLIC_BACK_END_URL ?? "",
  imageProviderUrl: process.env.NEXT_PUBLIC_IMAGE_PROVIDER_URL ?? ""
}